%a.right{:href => poems_songs_path} poems
#songs
  %table
    %thead
      %tr
        %th Title
        %th Recordings
        %th Comments
    %tbody
      - @songs.each do |song|
        %tr{ :class => cycle('even','odd') }
          %td= link_to song.title, song
          %td= link_to song.recordings.public.count, song
          %td= link_to song.comments.count, song

