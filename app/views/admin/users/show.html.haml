.admin_assistant.index.admin_songs
  .aa_header
    %h2
      =mail_to @user.email, @user.email
      \#
      = @user.id

  %p
    Single Access Token:
    = @user.single_access_token 

  %p
    Perishable Token:
    = @user.perishable_token    
    
  %p
    Last Login:
    = @user.last_login_at
  
  %p
    State:
    = @user.aasm_state          
    
  %p
    Last Notification:
    = Notification.find(@user.last_notification).title if !@user.last_notification.blank?